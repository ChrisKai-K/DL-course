# Put shared course dependencies here if most experiments use the same environment.
# If a project needs a different environment, add a dedicated requirements.txt inside that project.

numpy
torch
torchvision
matplotlib
tqdm
